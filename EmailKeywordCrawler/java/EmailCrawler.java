import java.net.*;import java.net.http.*;import java.util.*;import java.util.regex.*;import java.nio.file.*;
public class EmailCrawler{static final Pattern P=Pattern.compile("(?i)[a-z0-9._%+-]+\\s*(?:@|\\[at\\]|\\(at\\))\\s*[a-z0-9.-]+\\s*(?:\\.|\\[dot\\]|\\(dot\\))\\s*[a-z]{2,}");public static void main(String[]a)throws Exception{Set<String> out=new TreeSet<>();HttpClient c=HttpClient.newBuilder().followRedirects(HttpClient.Redirect.NORMAL).build();for(String u:a)if(u.startsWith("http")){var r=c.send(HttpRequest.newBuilder(URI.create(u)).header("User-Agent","EmailKeywordCrawler/1.0").build(),HttpResponse.BodyHandlers.ofString());for(Matcher m=P.matcher(r.body());m.find()){String e=m.group().toLowerCase().replaceAll("\\s+","").replace("[at]","@").replace("(at)","@").replace("[dot]",".").replace("(dot)",".");out.add(e);}System.out.println("{\"event\":\"page\",\"url\":\""+u+"\",\"emails\":"+out.size()+"}");}Files.writeString(Path.of("emails.txt"),String.join("\n",out));}}
